/**
 * 
 * @param {string} name 
 */
export const sayHello = name => {
    if (!name) {
        return "Hello, World"
    } else if (name === 'GuiGui'){
        return "Hello, Ass Hole"
    } else

    return `Hello, ${name}`
}
